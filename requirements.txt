Flask==3.0.0
flask-cors==4.0.0
# UPDATE to the latest stable version
supabase
python-dotenv==1.0.0
ariadne==0.22.0
# UPDATE to a more recent, compatible version
httpx
gunicorn # Ensure this is also present for the start command
